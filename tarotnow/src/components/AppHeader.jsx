// Re-export from shared component
// The shared AppHeader accepts: appId, colors, logo, title, subtitle, onLogoClick,
// useAuthHook, primaryAction, onLanguageToggle, languageLabel, themeToggle
export { default } from '@shared/components/AppHeader.jsx';
